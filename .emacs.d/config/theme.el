;; Color Scheme / Theme file

;; Currently, Emacs inherits the theme from the terminal, so I will not override it here (hence commented-out code below).
;(unless (package-installed-p 'solarized-theme)
;  (package-install 'solarized-theme))
;;; enable
;(eval-when-compile
;  (require 'solarized-theme))
;(load-theme 'solarized-dark t)

